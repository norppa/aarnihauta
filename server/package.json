{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "nodemon src/index.ts",
    "build:client": "npm run build --prefix ../client",
    "build:server": "tsc",
    "build:clean": "if exist dist (rmdir /s /q dist)",
    "build:pack": "xcopy /e /i ..\\clients\\web\\dist dist\\clients\\web && xcopy /e /i ..\\clients\\admin\\dist dist\\clients\\admin",
    "build": "npm run build:clean && npm run build:fronts && npm run build:backend && npm run build:pack"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "description": "",
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
